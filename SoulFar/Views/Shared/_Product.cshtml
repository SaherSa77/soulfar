@model SoulFar.Models.Product
<li>
    <figure>
        <a class="aa-product-img" href="@Url.Action("ViewDetails", "Product1", new { id = Model.ProductID })">
            <img src="@Url.Content(Model.PicturePath)" alt="" class="img-fluid img-thumbnail" >
        </a>
        <a class="aa-add-card-btn" href="@Url.Action("ViewDetails", "Product1", new { id = Model.ProductID })"><span class="fa fa-shopping-cart"></span>View Details</a>
        <figcaption>
            <h4 class="aa-product-title"><a href="#">@Model.Name</a></h4>
            <span class="aa-product-price">USD. @Model.UnitPrice</span><span class="aa-product-old-price">
                <del>
                    @if (Model.OldPrice != null)
                    {
                        <text>USD. @Model.OldPrice</text>
                    }
                </del>
            </span>
        </figcaption>
    </figure>
    <div class="aa-product-hvr-content">
        <a href="@(Session["username"]!=null ? Url.Action("WishList","Product1", new {id=@Model.ProductID }) :Url.Action("Login","Account"))" data-toggle="tooltip" data-placement="top" title="Add to Wishlist"><span class="fa fa-heart-o"></span></a>
        <a href="@Url.Action("ViewDetails", "Product1", new { id = Model.ProductID })" title="View Details" data-toggle="tooltip" data-placement="top"><span class="fa fa-info"></span></a>
    </div>
</li>



